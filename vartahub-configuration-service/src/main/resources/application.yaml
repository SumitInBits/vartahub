spring:
  application:
    name: vartahub-configuration-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:/config/global
            - classpath:/config/services


server:
  port: 8888

