spring:
  threads:
    virtual:
      enabled: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://${KEYCLOAK_EXTERNAL_HOST:localhost}:8090/realms/vartahub

  datasource:
    username: vartahub
    password: vartahub
